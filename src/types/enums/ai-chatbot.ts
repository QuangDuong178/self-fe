export enum AIChatType {
    USER_TYPED = "USER_TYPED",
    BOT_CONFIRM = "BOT_CONFIRM",
    BOT_TYPED = "BOT_TYPED",
    ERROR = "ERROR",
}

export enum AgentNoteType {
    SUGGEST = 0,
    IDEA= 1,
    OVERVIEW = 2,
    OVERVIEW_EXTEND = 3,
    DETAIL = 99,
    SOLUTION = 4,
}